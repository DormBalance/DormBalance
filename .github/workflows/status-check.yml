name: Status Check

on:
  pull_request:
    branches: ["main"]
  push:
    branches: ["main"]

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Pass
        run: echo "✅ Status check passed"
